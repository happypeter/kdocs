AM_INIT_AUTOMAKE(foreign)
AM_MAINTAINER_MODE

basically, if I want to use automake, I need the above calls.

automake is used to get Makefile.in out of Makefile.am
the configu.status will use Makefile.in to get Makefile
